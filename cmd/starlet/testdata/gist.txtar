-- bot.star --
def send_message(to, text, reply_markup = {}):
    return telegram.call(
        method = "sendMessage",
        args = {
            "chat_id": to,
            "text": text,
            "reply_markup": reply_markup
        }
    )

def handle(update):
    print(repr(update))
    send_message(bot_owner_id, "Hello, world!")
