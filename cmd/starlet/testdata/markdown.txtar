update: start
-- bot.star --
load("@starlet//tg.star", "tg")

def handle(update):
    tg.send_message(config.owner_id, "# Hello, world!\nI'm **Ilya**!")
    # Should be an error (invalid arguments).
    markdown.convert()
