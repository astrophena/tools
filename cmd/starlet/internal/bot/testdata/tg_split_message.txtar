update: start
-- bot.star --
load("@starlet//tg.star", "tg")

def handle(update):
    chunks = tg.split_message("x" * 5000)
    tg.send_message(
        config.owner_id,
        "{} {} {}".format(len(chunks), len(chunks[0]), len(chunks[1])),
    )
